# China-friendly mirrors for native binary downloads (electron, electron-builder).
# Override with env vars or a personal ~/.npmrc if these mirrors lag behind upstream.
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
